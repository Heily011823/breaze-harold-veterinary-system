apiVersion: v1
kind: Service
metadata:
  name: bh-core-service
  namespace: default
spec:
  selector:
    app: bh-core
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
  