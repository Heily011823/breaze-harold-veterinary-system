apiVersion: v1
kind: Service
metadata:
  name: bh-audit-service
  namespace: default
spec:
  selector:
    app: bh-audit
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP